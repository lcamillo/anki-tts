[project]
name = "anki-tts"
version = "1.0.0"
description = "Anki add-on for automatic text-to-speech during reviews"
authors = [
    {name = "Lucas Paulo de Lima Camillo"}
]
readme = "README.md"
requires-python = ">=3.9"
dependencies = [
    "edge-tts>=7.0.0",
]
